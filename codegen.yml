overwrite: true
# schema: "./src/typeDefs/Query.typeDef.gql"
schema: ./src/**/*.gql
generates:
  src/generated/graphql.d.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
